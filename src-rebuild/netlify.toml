[build]
  publish = "dist"
  command = "npm run build"

[dev]
  command = "npm run dev:vite"
  targetPort = 5173
  autoLaunch = false

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
